import { HigtlightDirective } from './higtlight.directive';

describe('HigtlightDirective', () => {
  it('should create an instance', () => {
    const directive = new HigtlightDirective();
    expect(directive).toBeTruthy();
  });
});
